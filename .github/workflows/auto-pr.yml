name: Auto PR

on:
  repository_dispatch:
    types: [deploy-complete]

jobs:
  create-pr:
    runs-on: ubuntu-latest

    steps:
      - name: Create PR (dev → main)
        id: create_pr
        run: |
          EXISTING=$(curl -s \
            -H "Authorization: Bearer ${{ secrets.AXA_GITHUB_TOKEN }}" \
            -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/${{ github.repository }}/pulls?state=open&head=axakorea-1996:dev&base=main" \
            | python3 -c "import sys,json; prs=json.load(sys.stdin); print(prs[0]['number'] if prs else '')")

          if [ -n "$EXISTING" ]; then
            echo "이미 열린 PR 있음: #$EXISTING, 스킵"
            echo "pr_number=$EXISTING" >> $GITHUB_OUTPUT
            exit 0
          fi

          PR_RESPONSE=$(curl -s -X POST \
            -H "Authorization: Bearer ${{ secrets.AXA_GITHUB_TOKEN }}" \
            -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/${{ github.repository }}/pulls" \
            -d "{
              \"title\": \"${{ github.event.client_payload.message }}\",
              \"head\": \"dev\",
              \"base\": \"main\",
              \"body\": \"## 🚀 자동 생성된 PR\n\n**커밋:** ${{ github.event.client_payload.sha }}\n**작성자:** ${{ github.event.client_payload.actor }}\n**브랜치:** dev → main\"
            }")

          PR_NUMBER=$(echo $PR_RESPONSE | python3 -c "import sys,json; print(json.load(sys.stdin)['number'])")
          echo "pr_number=$PR_NUMBER" >> $GITHUB_OUTPUT
          echo "PR 생성 완료: #$PR_NUMBER"

      - name: Trigger Webhook Manually
        if: steps.create_pr.outputs.pr_number != ''
        run: |
          curl -s -X POST http://${{ secrets.EC2_HOST }}/webhook/github \
            -H "Content-Type: application/json" \
            -H "X-GitHub-Event: pull_request" \
            -d "{
              \"action\": \"opened\",
              \"pull_request\": {
                \"number\": ${{ steps.create_pr.outputs.pr_number }},
                \"title\": \"${{ github.event.client_payload.message }}\"
              },
              \"repository\": {
                \"full_name\": \"${{ github.repository }}\"
              }
            }"
          echo "webhook 호출 완료"

      - name: Trigger UI Test
        if: steps.create_pr.outputs.pr_number != ''
        run: |
          curl -s -X POST \
            -H "Authorization: Bearer ${{ secrets.AXA_GITHUB_TOKEN }}" \
            -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/${{ github.repository }}/dispatches" \
            -d "{
              \"event_type\": \"auto-pr-complete\",
              \"client_payload\": {
                \"pr_number\": \"${{ steps.create_pr.outputs.pr_number }}\"
              }
            }"
          echo "UI 테스트 트리거 완료"